This folder contains the CPU performance counters and trace-points recorded for each neural network -based equalizer in one training epoch.

Open the files with Chrome or Firefox.

unit-lr.info presents a base number for the CPU cycles counter, as it shows the number of CPU cycles a unit linear regression model with almost no channel memory input incurs.  
